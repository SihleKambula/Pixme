import "@testing-library/jest-dom";
import { render, screen } from "@testing-library/react";
import ImageGrid from "../../components/ImageGrid";

it("Should return a list of images passed as data prop", () => {
  render(<ImageGrid />);
  expect();
});
